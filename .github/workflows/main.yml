on:
  schedule:
    - cron: '10 6 * * *'
      timezone: "Europe/Dublin"

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
